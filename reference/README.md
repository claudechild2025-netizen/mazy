# reference/

Drop the hi-fi prototype here as `mazy_prototype.html` (per `PROJECT_PLAN_v2.md` §1 step 4).

This directory is the visual source of truth. Pixel-level decisions
(spacing, exact illustration composition, mascot pose) should be
verified against this file before being treated as final.

> **Status (scaffold pass):** `mazy_prototype.html` is **not yet present**
> in this folder. The 14-screen happy-path scaffold was built from the
> textual cues in `PROJECT_PLAN_v2.md` §3 (tokens), §5 (per-screen notes),
> and §6 (build vs fake). Anywhere a pixel decision had to be guessed,
> the file contains a `// TODO(dw):` comment flagging it for re-verification.
